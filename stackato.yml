name: remo-dashboard

ignores:
  - ".git"
  - "*.pyc"
  - "*.pyo"
  - "*.swp"
  - "settings_local.py"
  - "/static"
  - "/bin/"
  - "/include/"
  - "/lib/"
  - "/local/"
  - "/man/"
  - "*~"

env:
  DJANGO_SETTINGS_MODULE: remo_dashboard.settings
  PIP_REQUIREMENTS_FILE: requirements.txt

mem: 128M

processes:
    web: $STACKATO_UWSGI --static-map /static=$HOME/remo_dashboard/static/

services:
    remo-dashboard: filesystem
    
hooks:
  post-staging:
    - python manage.py syncdb --noinput
    - python manage.py collectstatic --noinput
    - python manage.py migrate --noinput
    - python manage.py fetch_data profiles

framework:
  type: python
  start-file: remo_dashboard/wsgi.py
  runtime: python27
  
cron:
  - "* */6 * * * python $HOME/manage.py fetch_data profiles"